apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nginx
  namespace: nginx
spec:
  chart:
    spec:
      chart: nginx
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: nginx
      valuesFiles:
      - values.yaml
      version: 15.x
  interval: 1m0s
  targetNamespace: nginx
  values: {}
